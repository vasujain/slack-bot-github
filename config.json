{
    "version": "1.0",
    "admin_config" : {
        "slack" : {
            "slack_token_encrypted" : "Base64_encrypted_SLACK_TOKEN"
        },
        "github" : {
            "github_token_encrypted" : "Base64_encrypted_GITHUB_TOKEN"
        }
    }
}
